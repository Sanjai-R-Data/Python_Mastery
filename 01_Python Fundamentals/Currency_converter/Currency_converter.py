# Currency(INR to USD)

inr = float(input("Enter Amount(in INR): "))

usd = inr/95.66

print("Amount(in usd): ",round(usd,2))

# Currency(USD to INR)

usdd = float(input("\nEnter Amount(in USD): "))

inrr = usdd*95.66

print("Amount(in inr): ",round(inrr,2))